/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  // Ẩn logo Next.js góc màn hình khi chạy dev
  devIndicators: false,
}

export default nextConfig
